# Copia este archivo a .env.local y rellena los valores.

# URL pública del HF Space del Coach IA (ej. https://tu-usuario-forged-coach.hf.space)
# Si la dejas vacía, la web muestra un fallback en vez del iframe.
NEXT_PUBLIC_COACH_URL=

# (Opcional) Endpoint para el form de newsletter (POST { email }).
# Si la dejas vacía, el form abre el cliente de correo (mailto).
NEXT_PUBLIC_NEWSLETTER_ENDPOINT=
